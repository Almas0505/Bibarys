# 🚀 Быстрый старт для новичков

**Пошаговая инструкция для тех, кто впервые запускает веб-приложение.**

Не волнуйтесь! Мы пройдем каждый шаг вместе. Просто следуйте инструкциям по порядку.

# 🚀 Быстрый старт для новичков

**Пошаговая инструкция для тех, кто впервые запускает веб-приложение.**

Не волнуйтесь! Мы пройдем каждый шаг вместе. Просто следуйте инструкциям по порядку.

---

## 📌 ШАГ 0: Что вам понадобится

Это бесплатные программы, которые нужно установить на ваш компьютер:
- ✅ **Git** - для скачивания проекта
- ✅ **VS Code** - редактор кода (как блокнот, но для программистов)
- ✅ **Python 3.11 или 3.12** - для запуска backend части
- ✅ **Node.js** - для запуска frontend части

Не переживайте, мы установим всё вместе! 👇

---

## 📥 ШАГ 1: Установка необходимых программ

### 1.1 Установите Git

**Что это?** Программа для скачивания кода с GitHub.

**Windows:**
1. Перейдите на сайт: https://git-scm.com/download/win
2. Скачайте установщик (автоматически начнется загрузка)
3. Запустите установщик
4. Нажимайте "Next" везде (настройки по умолчанию подходят)
5. Дождитесь окончания установки

**Проверка:** Откройте командную строку (Win + R, введите `cmd`, Enter) и напишите:
```bash
git --version
```
Должно показать что-то вроде `git version 2.43.0`

---

### 1.2 Установите Visual Studio Code (VS Code)

**Что это?** Удобный редактор для работы с кодом. Бесплатный!

1. Перейдите на сайт: https://code.visualstudio.com/
2. Нажмите большую синюю кнопку **"Download for Windows"**
3. Запустите скачанный файл
4. Согласитесь с условиями
5. ✅ Обязательно отметьте галочку **"Add to PATH"**
6. Нажмите "Install"

---

### 1.3 Установите Python 3.11

**Что это?** Язык программирования для backend части проекта.

⚠️ **ВАЖНО:** Используйте Python 3.11 или 3.12 (НЕ 3.13!)

1. Перейдите на сайт: https://www.python.org/downloads/
2. Найдите **Python 3.11.9** (или 3.12.x)
3. Скачайте установщик для Windows
4. **ВАЖНО!** При установке:
   - ✅ Поставьте галочку **"Add Python to PATH"** (внизу окна)
   - Нажмите **"Install Now"**
5. Дождитесь окончания

**Проверка:** Откройте PowerShell (Win + X, выберите "Windows PowerShell") и напишите:
```bash
python --version
```
Должно показать: `Python 3.11.9` (или 3.12.x)

---

### 1.4 Установите Node.js

**Что это?** Нужен для запуска frontend части (сайта).

1. Перейдите на сайт: https://nodejs.org/
2. Скачайте **LTS версию** (зеленая кнопка слева)
3. Запустите установщик
4. Нажимайте "Next" везде
5. Дождитесь окончания

**Проверка:** В PowerShell напишите:
```bash
node --version
npm --version
```
Должно показать версии (например, `v20.11.0` и `10.2.4`)

---

## 🎉 Отлично! Все программы установлены. Теперь скачаем проект.

---

## 📂 ШАГ 2: Скачивание проекта

### 2.1 Откройте PowerShell
- Нажмите **Win + X**
- Выберите **"Windows PowerShell"**

### 2.2 Перейдите в папку, куда хотите скачать проект

Например, в папку "Документы":
```bash
cd $HOME\Documents
```

Или на Рабочий стол:
```bash
cd $HOME\Desktop
```

### 2.3 Скопируйте проект с GitHub

Вставьте эту команду и нажмите Enter:
```bash
git clone https://github.com/YOUR_USERNAME/Bibarys-main.git
```

⏳ Подождите... Проект скачивается...

✅ Когда появится новая строка - готово!

### 2.4 Откройте проект в VS Code

```bash
cd Bibarys-main
code .
```

VS Code откроется с вашим проектом! 🎊

---

## 🔧 ШАГ 3: Настройка Backend (Серверная часть)

### 3.1 Откройте терминал в VS Code

В VS Code:
- Нажмите **Ctrl + `** (это кнопка с галочкой слева от 1)
- Или в меню сверху: **Терминал → Новый терминал**

### 3.2 Перейдите в папку backend

```bash
cd backend
```

### 3.3 Создайте виртуальное окружение

**Что это?** Изолированная среда для Python пакетов. Не переживайте, это безопасно!

```bash
python -m venv venv
```

⏳ Подождите 10-20 секунд...

### 3.4 Активируйте виртуальное окружение

```bash
venv\Scripts\Activate.ps1
```

✅ Должно появиться **(venv)** в начале строки

❌ **Если появилась ошибка про политику выполнения:**
```bash
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```
Нажмите `Y` (да), затем повторите команду активации.

### 3.5 Установите зависимости

**Что это?** Библиотеки, которые нужны для работы backend.

```bash
pip install -r requirements.txt
```

⏳ Это займет 1-3 минуты. Подождите...

### 3.6 Создайте файл настроек .env

1. В VS Code в папке `backend` создайте новый файл:
   - Нажмите правой кнопкой на папку **backend**
   - Выберите **"Новый файл"**
   - Назовите его **.env** (именно так, с точкой в начале!)

2. Скопируйте в него это содержимое:

```env
# App
APP_NAME=Bibarys E-Commerce
DEBUG=true
API_V1_PREFIX=/api/v1

# Security
SECRET_KEY=supersecretkey123change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Database
DATABASE_URL=sqlite:///./ecommerce.db

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
```

3. Сохраните файл: **Ctrl + S**

### 3.7 Запустите Backend сервер! 🚀

```bash
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

✅ **Если видите:** `Application startup complete` - **ВСЁ РАБОТАЕТ!** 🎉

Не закрывайте это окно терминала! Сервер должен работать.

**Проверьте:** Откройте браузер и зайдите на http://localhost:8000/docs
Должна открыться документация API!

---

## 🎨 ШАГ 4: Настройка Frontend (Сайт)

### 4.1 Откройте НОВЫЙ терминал

В VS Code:
- Нажмите **Ctrl + Shift + `** (откроет второй терминал)
- Или нажмите **"+"** справа вверху в панели терминала

⚠️ **НЕ ЗАКРЫВАЙТЕ** первый терминал с backend!

### 4.2 Перейдите в папку frontend

```bash
cd frontend
```

### 4.3 Установите зависимости

```bash
npm install
```

⏳ Это займет 2-5 минут. Спокойно подождите...

☕ Можно сделать перерыв на чай!

### 4.4 Создайте файл настроек .env

1. В папке `frontend` создайте новый файл:
   - Правой кнопкой на **frontend**
   - **"Новый файл"**
   - Назовите **.env**

2. Скопируйте в него:

```env
VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_APP_NAME=Bibarys Shop
```

3. Сохраните: **Ctrl + S**

### 4.5 Запустите Frontend! 🚀

```bash
npm run dev
```

✅ **Если видите:** `Local: http://localhost:5173/` - **ГОТОВО!** 🎊

---

## 🎉 ШАГ 5: Откройте приложение!

### Откройте браузер и перейдите:
👉 **http://localhost:5173**

Вы должны увидеть главную страницу интернет-магазина!

---

## 🎯 ШАГ 6: Первое использование

### 6.1 Создайте аккаунт администратора

1. Нажмите кнопку **"Регистрация"** в правом верхнем углу
2. Заполните форму:
   - **Email:** admin@test.com
   - **Пароль:** admin123
   - **Имя:** Админ
   - **Фамилия:** Админов
   - **Телефон:** +77001234567
3. Нажмите **"Зарегистрироваться"**

### 6.2 Войдите в систему

1. Нажмите **"Вход"**
2. Введите email и пароль
3. Нажмите **"Войти"**

### 6.3 Создайте товар (как продавец)

1. Перейдите в **"Панель продавца"** (меню сверху)
2. Нажмите **"Добавить товар"**
3. Заполните форму:
   - Название
   - Описание
   - Цена
   - Количество
   - Категория
   - Загрузите фото (перетащите файлы)
4. Нажмите **"Создать"**

✅ Товар появится в каталоге!

---

## 📝 Полезные команды

### Остановить серверы:
- В терминале нажмите **Ctrl + C**

### Запустить заново:

**Backend:**
```bash
cd backend
venv\Scripts\Activate.ps1
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**Frontend:**
```bash
cd frontend
npm run dev
```

---

## ❓ Частые проблемы

### ❌ "Python не найден"
- Переустановите Python с галочкой **"Add to PATH"**

### ❌ "npm не найден"
- Переустановите Node.js

### ❌ Backend не запускается
- Проверьте, активировали ли venv: должно быть **(venv)** в начале строки
- Убедитесь, что установлены зависимости: `pip install -r requirements.txt`

### ❌ Frontend не запускается
- Убедитесь, что установлены зависимости: `npm install`
- Проверьте, что backend работает на порту 8000

### ❌ Порт 8000 или 5173 занят
Backend:
```bash
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8001
```
Frontend: измените порт в vite.config.ts

---

## 🎓 Что дальше?

Теперь у вас работает полноценный интернет-магазин!

**Можете:**
- ✅ Создавать товары
- ✅ Добавлять в корзину
- ✅ Оформлять заказы
- ✅ Управлять пользователями (админ панель)
- ✅ Просматривать статистику

**Изучите:**
- Backend API: http://localhost:8000/docs
- Структуру проекта в VS Code
- Код компонентов в папке `frontend/src`

---

## 🆘 Нужна помощь?

- 📖 Полная документация: см. README.md
- 🚀 Деплой на сервер: см. DEPLOYMENT.md  
- 💬 Есть вопросы? Создайте Issue на GitHub

---

## ✅ Чеклист для проверки

- [ ] Git установлен
- [ ] VS Code установлен  
- [ ] Python 3.11/3.12 установлен
- [ ] Node.js установлен
- [ ] Проект скачан через git clone
- [ ] Backend запущен (http://localhost:8000/docs открывается)
- [ ] Frontend запущен (http://localhost:5173 открывается)
- [ ] Создан аккаунт
- [ ] Создан первый товар

**Если все отмечено ✅ - ПОЗДРАВЛЯЕМ! Вы справились!** 🎊

---

⭐ **Понравился проект? Поставьте звезду на GitHub!**
  "email": "seller@example.com",
  "password": "seller123",
  "first_name": "Seller",
  "last_name": "User",
  "role": "seller"
}
```

---

## 🐋 Docker (альтернативный способ)

Если у вас установлен Docker:

```bash
# Запустите всё приложение
docker-compose up -d

# Backend будет на: http://localhost:8000
# Frontend будет на: http://localhost:3000
```

---

## 🔍 API Документация

После запуска backend, откройте:
- **Swagger UI:** http://localhost:8000/docs
- **ReDoc:** http://localhost:8000/redoc

Здесь вы можете:
- Просмотреть все доступные эндпоинты
- Протестировать API прямо в браузере
- Увидеть схемы запросов и ответов

---

## 🛠️ Полезные команды

### Backend:
```bash
# Запуск с auto-reload
uvicorn app.main:app --reload

# Запуск на другом порту
uvicorn app.main:app --port 8001

# Создание миграций (если используете Alembic)
alembic revision --autogenerate -m "Initial migration"
alembic upgrade head
```

### Frontend:
```bash
# Запуск dev сервера
npm run dev

# Production build
npm run build

# Предпросмотр production build
npm run preview

# Линтинг кода
npm run lint
```

---

## ❓ Решение проблем

### Python 3.13 ошибка:
```
AssertionError in SQLAlchemy TypingOnly
```
**Решение:** Используйте Python 3.11 или 3.12

### Port уже используется:
```
Port 8000 is already in use
```
**Решение:** 
```bash
# Windows: найдите процесс
netstat -ano | findstr :8000
# Убейте процесс по PID
taskkill /PID <pid> /F

# Linux/Mac:
lsof -ti:8000 | xargs kill -9
```

### CORS ошибки:
Убедитесь, что в backend `.env` файле:
```env
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
```

### npm install ошибки:
```bash
# Очистите кеш
npm cache clean --force

# Удалите node_modules и package-lock.json
rm -rf node_modules package-lock.json

# Установите заново
npm install
```

---

## 📚 Дополнительная документация

- **Backend README:** `backend/README.md`
- **Frontend README:** `frontend/README.md`
- **API Docs:** http://localhost:8000/docs
- **Основной README:** `README.md`

---

## ✅ Проверка установки

Проверьте, что всё работает:

1. ✅ Backend запущен: http://localhost:8000
2. ✅ API Docs доступны: http://localhost:8000/docs
3. ✅ Frontend запущен: http://localhost:5173
4. ✅ Можно зарегистрироваться
5. ✅ Можно войти в систему

---

**Готово! Приложение работает! 🎉**

Если возникли проблемы, проверьте:
- Версию Python (должна быть 3.11 или 3.12)
- Активировано ли виртуальное окружение
- Созданы ли `.env` файлы
- Установлены ли все зависимости
