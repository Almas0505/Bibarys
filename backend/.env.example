# Application
APP_NAME=SaudaFlow API
APP_VERSION=1.0.0
DEBUG=True

# Server
HOST=0.0.0.0
PORT=8000

# Database
# По умолчанию используется абсолютный путь из app/config.py -> backend/ecommerce.db.
# Раскомментируйте строку ниже, только если нужен другой путь (НЕ используйте относительный путь,
# иначе запуск из разных директорий создаст разные файлы БД).
# DATABASE_URL=sqlite:///./ecommerce.db
# PostgreSQL (production)
# DATABASE_URL=postgresql://user:password@localhost:5432/ecommerce

# JWT Security
SECRET_KEY=your-secret-key-change-in-production-min-32-characters-long
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Email (optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@bibarys.com
EMAILS_FROM_EMAIL=
FRONTEND_URL=http://localhost:3000

# Payment (optional)
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
